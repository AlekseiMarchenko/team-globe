Quick deploy:
1) In your repo **team-globe**, open **Add file → Upload files**.
2) Upload this `index.html` (replace the old one) and **Commit**.
3) Visit `https://<your-user>.github.io/team-globe/` (append `?autogif=1` to auto-record).
